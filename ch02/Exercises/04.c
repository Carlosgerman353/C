#include <stdio.h>

int main(void) {
    int some, value; 
    float create, something; 

    printf("%d\n%d\n%f\n%f", some, value, create, something);
    return 0;
}

// No patters discovered. The output looks random