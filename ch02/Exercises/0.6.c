#include <stdio.h>

int main(void) {
    /*
        Not a good idea to have more than one adjecent underscore
        such as current__balance because it could be confusing when
        reusing, aka harder to read.
    */
    return 0;
}